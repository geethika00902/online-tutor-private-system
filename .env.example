# Server
SERVER_PORT=3001

# Database (MySQL)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=tutor_system
DB_PORT=3306

# Policy
# Minutes within which a session cannot be cancelled (e.g., 1440 = 24h)
POLICY_CANCEL_LOCK_MINUTES=1440
# Max minutes a student can book per day (0 disables limit)
POLICY_MAX_DAILY_BOOKING_MINUTES=360